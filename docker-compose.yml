services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      APP_ENV: ${APP_ENV:-local}
      DEFAULT_ROLE: ${DEFAULT_ROLE:-fde_admin}
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
